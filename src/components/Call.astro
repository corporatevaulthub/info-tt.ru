---
import { Image } from 'astro:assets';
import CallForm from '@components/forms/CallForm';
import { getEntry } from 'astro:content';
const { data } = await getEntry('home', 'call');
---

<div class="callback py-10 md:py-20" id="callback">
  <div class="container">
    <div class="mb-10 flex flex-col items-end justify-between gap-5 md:flex-row">
      <h2 class="section-title text-white">{data.title}</h2>
      <p class="font-semibold text-white">{data.description}</p>
    </div>
    <div class="bg-gray-50 relative px-5 py-10 md:px-16">
      <CallForm client:load />
      <Image src={import('@assets/form.png')} quality={100} class="absolute bottom-0 right-0 hidden xl:block" alt="png" />
    </div>
  </div>
</div>
<style>
  .callback {
    position: relative;
  }
  .callback::after {
    content: '';
    z-index: -1;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(180deg, #b2ddfd 0%, #4764b9 100%), linear-gradient(182deg, #43aaf5 1.35%, #223c86 105.4%), linear-gradient(180deg, #0e88e1 0%, #203984 100%), linear-gradient(180deg, #0e43e1 0%, #082782 100%);
    transform: rotate(-180deg);
  }
</style>
